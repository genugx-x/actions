
name: test

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run Gretting!
        run: echo Hello, master!
      - name: Run Goodbye!
        run: echo Goodbye, master!
