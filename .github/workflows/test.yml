
name: test

on:
#  push:
#    branches: [ "master" ]
  pull_request:
    branches: [ "dev" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run Gretting!
        run: echo Hello, dev!
      - name: Run Goodbye!
        run: echo Goodbye, dev!
